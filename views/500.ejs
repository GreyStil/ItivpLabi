<%- include('partials/header') %>

  <section>
    <h2>500 — Ошибка сервера</h2>
    <p><%= error && error.message ? error.message : 'Неожиданная ошибка' %></p>
    <div class="actions"><a class="btn secondary" href="/">На главную</a></div>
  </section>

<%- include('partials/footer') %>
